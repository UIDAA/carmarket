server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # BrowserRouter(react-router)를 쓰므로 /search, /cars/1 같은 경로를 새로고침해도
  # 404 없이 index.html을 내려주고 클라이언트 라우팅이 처리하게 한다.
  location / {
    try_files $uri $uri/ /index.html;
  }
}
