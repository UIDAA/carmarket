# 이 파일을 .env로 복사해서 실제 값을 채우면 docker-compose.yml이 자동으로 읽는다.
# .env 자체는 git에 올라가지 않는다(.gitignore 참고).

JWT_SECRET=carmarket-docker-dev-secret
VITE_API_BASE_URL=http://localhost:4000
GEMINI_API_KEY=여기에_발급받은_키
